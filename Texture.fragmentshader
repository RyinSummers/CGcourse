#version 330 core

// 从顶点着色器输入UV坐标
in vec2 fragmentUV;

// 输出颜色
out vec3 color;

// 纹理样式
uniform sampler2D myTextureSampler;

void main(){

	color = texture( myTextureSampler, fragmentUV ).rgb;
}